# Production builds must use the live HRM API. These values are public runtime
# configuration, not credentials; deployment secrets remain on the API only.
VITE_HRM_API_BASE=/api
VITE_USE_REAL_API=true
VITE_HRM_TENANT_ID=local-tenant
